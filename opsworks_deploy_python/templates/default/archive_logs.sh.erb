#!/bin/bash

log_file_ext=$1

gzip -c /var/log/<%= @application %>/*.$log_file_ext > /tmp/log.gz

aws s3 cp /tmp/log.gz s3://<%= @application %>-worm/<%= @application %>/$(date +%Y-%m-%dT%H:%M:%SZ).log.gz
